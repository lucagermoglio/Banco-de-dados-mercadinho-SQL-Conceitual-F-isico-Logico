CREATE DATABASE Mercadinho;
USE Mercadinho;

CREATE TABLE Fornecedores (
    id_fornecedor INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    telefone VARCHAR(20),
    email VARCHAR(100)
);

CREATE TABLE Produtos (
    id_produto INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    categoria VARCHAR(50),
    quantidade_estoque INT NOT NULL,
    preco DECIMAL(10,2) NOT NULL,
    id_fornecedor INT,
    FOREIGN KEY (id_fornecedor) REFERENCES Fornecedores(id_fornecedor)
);

CREATE TABLE Clientes (
    id_cliente INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    telefone VARCHAR(20),
    endereco VARCHAR(200)
);

CREATE TABLE Vendas (
    id_venda INT AUTO_INCREMENT PRIMARY KEY,
    data_venda DATETIME DEFAULT CURRENT_TIMESTAMP,
    id_cliente INT,
    valor_total DECIMAL(10,2),
    FOREIGN KEY (id_cliente) REFERENCES Clientes(id_cliente)
);

CREATE TABLE Itens_Venda (
    id_item INT AUTO_INCREMENT PRIMARY KEY,
    id_venda INT NOT NULL,
    id_produto INT NOT NULL,
    quantidade INT NOT NULL,
    preco_unitario DECIMAL(10,2) NOT NULL,
    subtotal DECIMAL(10,2) NOT NULL,
    FOREIGN KEY (id_venda) REFERENCES Vendas(id_venda),
    FOREIGN KEY (id_produto) REFERENCES Produtos(id_produto)
);

INSERT INTO Fornecedores (nome, telefone, email)
VALUES
('São Braz', '11999999999', 'saobrazcontato@distribuidora.com'),
('Bebidas Brasil', '11888888888', 'vendas@bebidas.com');

INSERT INTO Produtos (nome, categoria, quantidade_estoque, preco, id_fornecedor)
VALUES
('Arroz 5kg', 'Alimentos', 50, 24.90, 1),
('Feijão 1kg', 'Alimentos', 80, 7.50, 1),
('Refrigerante 2L', 'Bebidas', 40, 9.99, 2);

INSERT INTO Clientes (nome, telefone, endereco)
VALUES
('João Silva', '11977777777', 'Rua A, 100'),
('Maria Souza', '11966666666', 'Rua B, 200');

INSERT INTO Vendas (id_cliente, valor_total)
VALUES (1, 42.39);

INSERT INTO Itens_Venda (id_venda, id_produto, quantidade, preco_unitario, subtotal)
VALUES
(1, 1, 1, 24.90, 24.90),
(1, 2, 1, 7.50, 7.50),
(1, 3, 1, 9.99, 9.99);

SELECT * FROM Produtos;

-- Consultar estoque (menos de 10 unidades)
SELECT * FROM Produtos
WHERE quantidade_estoque < 10;

SELECT
    V.id_venda,
    C.nome AS cliente,
    V.data_venda,
    V.valor_total
FROM Vendas V
JOIN Clientes C ON V.id_cliente = C.id_cliente;